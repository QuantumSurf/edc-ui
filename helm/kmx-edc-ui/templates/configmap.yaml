apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kmx-edc-ui.fullname" . }}-config
  labels:
    {{- include "kmx-edc-ui.labels" . | nindent 4 }}
data:
  edc-default-url: {{ .Values.edc.defaultUrl | quote }}
